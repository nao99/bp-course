spring:
  application:
    name: 'BP-Course'

  profiles:
    active: 'prod'

  datasource:
    driver-class-name: 'org.postgresql.Driver'
    url: 'jdbc:postgresql://database/database_name'
    username: 'user'
    password: 'password'
